---
import Layout from "../layouts/Layout.astro";
import Waveform from "../components/Waveform.astro";
import Pipeline from "../components/Pipeline.astro";

const REPO = "https://github.com/apphane-dev/ses";

const features = [
  {
    tag: "garble gate",
    title: "Retry the bad takes",
    body: "A sustained loud burst detector catches likely garbling per chunk and re-rolls it within a configurable retry budget.",
  },
  {
    tag: "pause gate",
    title: "Catch dead air",
    body: "Internal silences that run too long trigger a re-render of the whole section before the take is ever accepted.",
  },
  {
    tag: "raw / enhanced",
    title: "Enhance without losing the clean take",
    body: "LavaSR post processing is optional. If it injects high frequency hash, the enhanced result is discarded and the raw take is kept.",
  },
  {
    tag: "--slide N",
    title: "Iterate one section at a time",
    body: "Revise a single part with --slide N instead of re-rendering the entire script. Indexes are zero based.",
  },
];

const chips = [
  "Python",
  "SoX for rendering",
  "Local model weights",
  "Apple MPS or CPU",
  "Command-line workflow",
];

const docs = [
  { label: "Quickstart & script format", meta: "README", href: `${REPO}#quickstart` },
  { label: "Architecture & module map", meta: "docs/DESIGN.md", href: `${REPO}/blob/main/docs/DESIGN.md` },
  { label: "Contribution & testing policy", meta: "docs/CONTRIBUTING.md", href: `${REPO}/blob/main/docs/CONTRIBUTING.md` },
  { label: "Complete narration example", meta: "examples/script.md", href: `${REPO}/blob/main/examples/script.md` },
  { label: "Voice-sample setup", meta: "examples/voices/", href: `${REPO}/blob/main/examples/voices/README.md` },
];

const faqs = [
  {
    q: "Does ses need a GPU?",
    a: "No. It runs Qwen3-TTS locally on Apple MPS or CPU. Model weights download on the first render and are reused after that.",
  },
  {
    q: "Can I render offline?",
    a: "Yes, once the model weights are cached. Later runs need no network access.",
  },
  {
    q: "What kind of voice sample works best?",
    a: "A clean clip of ten seconds or more. Pair the sample with a transcript to enable ICL cloning for the closest match.",
  },
  {
    q: "One section came out wrong. Do I re-render everything?",
    a: "No. Pass --slide N to render a single section and keep every accepted take untouched.",
  },
  {
    q: "Can I swap in a different TTS engine?",
    a: "Engines sit behind small Protocols, so you can pass your own engine to run_pipeline. The quality gates are tuned to this stack, so a different engine usually needs different thresholds.",
  },
  {
    q: "Is it free for commercial work?",
    a: "Yes. ses is MIT licensed with no usage restrictions.",
  },
];

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((f) => ({
    "@type": "Question",
    name: f.q,
    acceptedAnswer: { "@type": "Answer", text: f.a },
  })),
};

const taglineWords =
  "Every section comes back editable. Every suspicious take gets caught before it ships.".split(" ");
---

<Layout>
  <header class="nav">
    <div class="nav-pill">
      <a class="brand" href="/" aria-label="ses home">
        <span class="brand-mark" aria-hidden="true">
          <i></i><i></i><i></i><i></i><i></i>
        </span>
        <span class="brand-name">ses</span>
        <span class="brand-sub mono">/ voice</span>
      </a>
      <nav class="nav-links mono" aria-label="Primary">
        <a href="#workflow">Pipeline</a>
        <a href="#script">Script</a>
        <a href="#start">Quickstart</a>
        <a class="nav-cta" href={REPO} rel="noopener">GitHub ↗</a>
      </nav>
    </div>
  </header>

  <main id="main">
    <!-- ============ HERO ============ -->
    <section class="hero">
      <div class="wrap hero-inner" data-reveal>
        <p class="eyebrow">Open-source voice-cloning voiceover pipeline</p>
        <h1 class="hero-title">
          Turn a voice sample and<br />a Markdown script into<br />organized narration.
        </h1>
        <p class="hero-body">
          <span class="mono kw">ses</span> renders a cloned voice section by
          section, retries suspicious takes automatically, enhances the result
          optionally, and hands you editable WAVs plus one complete render.
        </p>

        <div class="hero-cta">
          <a class="btn btn-primary" href="#start">Read the quickstart</a>
          <a class="btn btn-ghost" href="#workflow">Explore the pipeline</a>
        </div>

        <ul class="proof mono" aria-label="At a glance">
          <li>Local Python CLI</li>
          <li>Qwen3-TTS</li>
          <li>LavaSR</li>
          <li>MIT licensed</li>
        </ul>
      </div>

      <div class="hero-scope wrap" aria-hidden="true" data-reveal>
        <div class="scope">
          <div class="scope-label mono"><span class="dot"></span> voiced signal · in → out</div>
          <div class="wave-slot"><Waveform bars={120} /></div>
          <div class="scope-ticks mono">
            <span>0:00</span><span>0:20</span><span>0:45</span><span>full.wav</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ============ INPUT → OUTPUT STRIP ============ -->
    <section class="section-pad strip-sec">
      <div class="wrap">
        <div class="strip">
          <div class="strip-node" data-reveal>
            <span class="strip-k mono">input</span>
            <span class="strip-v">Voice sample + transcript</span>
            <span class="strip-note">ICL cloning, or audio alone</span>
          </div>
          <span class="strip-arrow mono" aria-hidden="true">→</span>
          <div class="strip-node" data-reveal>
            <span class="strip-k mono">input</span>
            <span class="strip-v">Markdown script</span>
            <span class="strip-note">Timestamped sections</span>
          </div>
          <span class="strip-arrow mono" aria-hidden="true">→</span>
          <div class="strip-node" data-reveal>
            <span class="strip-k mono">process</span>
            <span class="strip-v">Quality gated pipeline</span>
            <span class="strip-note">Your pacing &amp; thresholds</span>
          </div>
          <span class="strip-arrow mono" aria-hidden="true">→</span>
          <div class="strip-node out" data-reveal>
            <span class="strip-k mono">output</span>
            <span class="strip-v">Section WAVs + full.wav</span>
            <span class="strip-note">Every section is editable</span>
          </div>
        </div>
        <p class="strip-caption" data-reveal>
          Bring any voice sample and any script. Pair the sample with a
          transcript for ICL cloning, tune the pacing to your project, and keep
          every section as a separate deliverable.
        </p>
      </div>
    </section>

    <!-- ============ TAGLINE REVEAL ============ -->
    <section class="section-pad tagline-sec" aria-label="Why ses">
      <div class="wrap">
        <p class="tagline" data-tagline>
          {taglineWords.map((w, i) => <span data-word style={`--i:${i}`}>{w}{" "}</span>)}
        </p>
      </div>
    </section>

    <!-- ============ FEATURES ============ -->
    <section class="section-pad" id="features">
      <div class="wrap">
        <div class="head-block" data-reveal>
          <p class="eyebrow">Built for narration work</p>
          <h2 class="h2">Not just a text-to-speech demo.</h2>
        </div>
        <div class="feature-grid">
          {
            features.map((f) => (
              <article class="card" data-reveal>
                <span class="card-tag mono">{f.tag}</span>
                <h3 class="card-title">{f.title}</h3>
                <p class="card-body">{f.body}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ============ WORKFLOW ============ -->
    <section class="section-pad workflow-sec" id="workflow">
      <div class="wrap">
        <div class="head-block" data-reveal>
          <p class="eyebrow">The signal path</p>
          <h2 class="h2">From Markdown to final voiceover.</h2>
          <p class="head-sub">
            The orchestration is one line in
            <span class="mono kw">ses/pipeline.py</span>:
            <span class="mono">parse → chunk → synth → gate → enhance → level → concat.</span>
            Two stations can send the signal backwards.
          </p>
        </div>
        <Pipeline />
        <div class="loop-legend mono" data-reveal>
          <span><i class="lg lg-signal"></i> forward signal</span>
          <span><i class="lg lg-loop"></i> retry loop</span>
        </div>
      </div>
    </section>

    <!-- ============ SCRIPT SHOWCASE ============ -->
    <section class="section-pad" id="script">
      <div class="wrap wide">
        <div class="head-block" data-reveal>
          <p class="eyebrow">Authoring</p>
          <h2 class="h2">Write narration in plain Markdown.</h2>
          <p class="head-sub">
            Timestamped headings become named audio files. Blockquotes become
            narration. Blank quoted lines create longer paragraph pauses. Leave
            <span class="mono">TODO</span>, <span class="mono">REDO</span>, or
            <span class="mono">WIP</span> markers in place and
            <span class="mono kw">ses</span> skips them.
          </p>
        </div>

        <div class="script-grid">
          <figure class="code-pane" data-reveal>
            <figcaption class="pane-bar mono">
              <span class="pane-dots" aria-hidden="true"><i></i><i></i><i></i></span>
              <span>script.md</span>
            </figcaption>
            <pre class="code"><code><span class="c-h">## 0:00 — Welcome</span>

<span class="c-q">&gt; Welcome to ses, a voiceover pipeline. This tool turns a</span>
<span class="c-q">&gt; written script into narrated audio using a short sample</span>
<span class="c-q">&gt; of your own voice.</span>
<span class="c-q">&gt;</span>
<span class="c-q">&gt; You write in plain markdown, drop in a voice sample, and</span>
<span class="c-q">&gt; get back one WAV per section plus a single render.</span>

<span class="c-h">## 0:20 — How it works</span>

<span class="c-q">&gt; Each section is split into sentences and synthesized one</span>
<span class="c-q">&gt; sentence at a time, which keeps the prosody stable.</span>
<span class="c-q">&gt;</span>
<span class="c-mark">&gt; TODO</span> <span class="c-c">tighten the pacing note here</span></code></pre>
          </figure>

          <figure class="code-pane" data-reveal>
            <figcaption class="pane-bar mono">
              <span class="pane-tree" aria-hidden="true">▚</span>
              <span>audio/20260720-143000/</span>
            </figcaption>
            <pre class="code tree"><code><span class="t-dir">audio/20260720-143000/</span>
├─ <span class="t-file">slide-00-welcome.wav</span>       <span class="t-meta">§ 0:00</span>
├─ <span class="t-file">slide-01-how-it-works.wav</span>  <span class="t-meta">§ 0:20</span>
├─ <span class="t-file">slide-02-getting…wav</span>       <span class="t-meta">§ 0:45</span>
├─ <span class="t-raw">slide-02-getting….raw.wav</span> <span class="t-meta">raw take kept</span>
└─ <span class="t-full">full.wav</span>                    <span class="t-meta">every section, joined</span></code></pre>
          </figure>
        </div>
      </div>
    </section>

    <!-- ============ QUICKSTART ============ -->
    <section class="section-pad start-sec" id="start">
      <div class="wrap wide start-grid">
        <div class="start-copy" data-reveal>
          <p class="eyebrow">Your first render</p>
          <h2 class="h2">Three commands to a full run.</h2>
          <p class="head-sub">
            Put <span class="mono">my_voice.wav</span> in <span class="mono">in/</span>.
            Add <span class="mono">my_voice.txt</span> with the clip transcript to
            enable ICL cloning. Model weights download on the first render; later
            runs can work offline.
          </p>

          <div class="cmd-list">
            <p class="cmd-lead mono">Useful controls</p>
            <div class="cmd"><code class="mono">--dry-run</code><span>Parse and print, no models and no audio.</span></div>
            <div class="cmd"><code class="mono">--slide 2</code><span>Render a single section (zero based).</span></div>
            <div class="cmd"><code class="mono">--no-enhance</code><span>Render without LavaSR enhancement.</span></div>
            <div class="cmd"><code class="mono">--samples-dir</code><span>Point at any folder of voice samples.</span></div>
          </div>
        </div>

        <figure class="code-pane term" data-reveal>
          <figcaption class="pane-bar mono">
            <span class="pane-dots" aria-hidden="true"><i></i><i></i><i></i></span>
            <span>zsh — first render</span>
          </figcaption>
          <pre class="code"><code><span class="c-c"># 1 · install dependencies</span>
<span class="c-p">$</span> pip install -r requirements.txt
<span class="c-c"># qwen-tts shells out to the SoX executable</span>
<span class="c-p">$</span> brew install sox

<span class="c-c"># 2 · drop in/my_voice.wav (+ optional in/my_voice.txt)</span>
<span class="c-c">#     then render your script</span>
<span class="c-p">$</span> python -m ses <span class="c-flag">--script</span> script.md <span class="c-flag">--sample</span> my_voice

<span class="c-c"># iterate on just section 2, no enhancement</span>
<span class="c-p">$</span> python -m ses <span class="c-flag">--script</span> script.md <span class="c-flag">--sample</span> my_voice \
    <span class="c-flag">--slide</span> 2 <span class="c-flag">--no-enhance</span></code></pre>
        </figure>
      </div>
    </section>

    <!-- ============ ARCHITECTURE + COPY-OUT ============ -->
    <section class="section-pad arch-sec" id="architecture">
      <div class="wrap wide arch-grid">
        <div class="arch-copy" data-reveal>
          <p class="eyebrow">For developers</p>
          <h2 class="h2">Small modules. Explicit seams. No plugin magic.</h2>
          <p class="head-sub">
            Parsing, chunking, DSP, and quality gates stay importable with numpy
            alone. Heavy model libraries are isolated inside adapters. Use the
            whole pipeline, pass your own engine to
            <span class="mono kw">run_pipeline</span>, or copy only the modules
            your project needs.
          </p>
          <ul class="arch-list">
            <li>Frozen <span class="mono">PipelineConfig</span> — every tunable in one place.</li>
            <li>Tiny <span class="mono">Protocol</span>s for parsers, TTS engines, and enhancers.</li>
            <li>No global state, no engine auto-discovery.</li>
            <li>Pure-numpy tests with synthetic tones, bursts, and silence.</li>
          </ul>
        </div>

        <div class="copyout" data-reveal>
          <p class="copyout-title">Take the pipeline, or just the useful parts.</p>
          <p class="copyout-body">
            Need only trim, fade, normalize, garble detection, or pause
            detection? The pure audio modules copy into another project without
            torch, Qwen3-TTS, LavaSR, or the orchestration layer.
          </p>
          <figure class="code-pane term small">
            <figcaption class="pane-bar mono">
              <span class="pane-dots" aria-hidden="true"><i></i><i></i><i></i></span>
              <span>copy-out</span>
            </figcaption>
            <pre class="code"><code><span class="c-p">$</span> mkdir -p yourproject/audio
<span class="c-p">$</span> cp ses/audio/__init__.py \
     ses/audio/dsp.py ses/audio/quality.py \
     yourproject/audio/
<span class="c-p">$</span> pip install numpy</code></pre>
          </figure>
        </div>
      </div>
    </section>

    <!-- ============ HONEST FIT ============ -->
    <section class="section-pad fit-sec">
      <div class="wrap">
        <div class="fit-inner" data-reveal>
          <div class="fit-head">
            <p class="eyebrow">The honest part</p>
            <h2 class="h2">A local developer tool with a deliberate engine stack.</h2>
          </div>
          <p class="fit-body">
            <span class="mono kw">ses</span> runs Qwen3-TTS locally on Apple MPS
            or CPU and uses LavaSR for optional enhancement. The quality gates
            are tuned to this stack. You can replace an adapter, but a different
            engine will usually need different gate thresholds. It is an
            early-stage, developer-oriented reference pipeline, not a hosted
            product.
          </p>
          <ul class="chips mono" aria-label="Requirements">
            {chips.map((c) => <li>{c}</li>)}
          </ul>
        </div>
      </div>
    </section>

    <!-- ============ DOCS ============ -->
    <section class="section-pad" id="docs">
      <div class="wrap">
        <div class="head-block" data-reveal>
          <p class="eyebrow">Read the source</p>
          <h2 class="h2">Everything is documented in the repo.</h2>
        </div>
        <ul class="doc-list" data-reveal>
          {
            docs.map((d, i) => (
              <li>
                <a class="doc-row" href={d.href} rel="noopener">
                  <span class="doc-idx mono">{String(i + 1).padStart(2, "0")}</span>
                  <span class="doc-label">{d.label}</span>
                  <span class="doc-meta mono">{d.meta}</span>
                  <span class="doc-arrow mono" aria-hidden="true">↗</span>
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </section>

    <!-- ============ FAQ ============ -->
    <section class="section-pad faq-sec" id="faq">
      <div class="wrap">
        <div class="head-block" data-reveal>
          <p class="eyebrow">Before you commit</p>
          <h2 class="h2">Common questions, answered straight.</h2>
        </div>
        <div class="faq-grid">
          {
            faqs.map((f) => (
              <div class="faq-item" data-reveal>
                <h3 class="faq-q">{f.q}</h3>
                <p class="faq-a">{f.a}</p>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ============ FINAL CTA ============ -->
    <section class="section-pad cta-sec">
      <div class="wrap cta-inner" data-reveal>
        <div class="wave-slot cta-wave"><Waveform bars={72} /></div>
        <h2 class="cta-title">Give your script a voice,<br />one section at a time.</h2>
        <p class="cta-body">
          Start with a reference clip, write the narration in Markdown, and let
          <span class="mono kw"> ses</span> render, check, enhance, and assemble the take.
        </p>
        <div class="hero-cta center">
          <a class="btn btn-primary" href={`${REPO}#quickstart`} rel="noopener">Read the quickstart</a>
          <a class="btn btn-ghost" href={REPO} rel="noopener">View on GitHub ↗</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="foot">
    <div class="wrap foot-inner">
      <div class="foot-brand">
        <span class="brand-name">ses</span>
        <span class="brand-sub mono">/ voice · Turkish</span>
      </div>
      <p class="foot-note mono">No tracking, no cookies. Open source under the <a href={`${REPO}/blob/main/LICENSE`} rel="noopener">MIT License</a>.</p>
      <a class="foot-link mono" href={REPO} rel="noopener">github.com/apphane-dev/ses ↗</a>
    </div>
  </footer>

  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</Layout>

<style>
  /* ---------- fluid island nav ---------- */
  .nav {
    position: fixed;
    top: 24px;
    left: 0;
    right: 0;
    z-index: 40;
    display: flex;
    justify-content: center;
    pointer-events: none;
  }
  .nav-pill {
    pointer-events: auto;
    display: flex;
    align-items: center;
    gap: 32px;
    max-width: calc(100vw - 32px);
    padding: 8px 12px 8px 16px;
    border-radius: 999px;
    border: 1px solid var(--line);
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
  }
  .brand {
    display: inline-flex;
    align-items: baseline;
    gap: 8px;
    text-decoration: none;
  }
  .brand-mark {
    display: inline-flex;
    align-items: flex-end;
    gap: 2px;
    height: 16px;
    align-self: center;
  }
  .brand-mark i {
    width: 3px;
    border-radius: 2px;
    background: var(--brass);
    animation: navpulse 1.8s var(--ease) infinite;
  }
  .brand-mark i:nth-child(1) { height: 40%; animation-delay: 0s; }
  .brand-mark i:nth-child(2) { height: 80%; animation-delay: 0.15s; }
  .brand-mark i:nth-child(3) { height: 55%; animation-delay: 0.3s; }
  .brand-mark i:nth-child(4) { height: 100%; animation-delay: 0.45s; }
  .brand-mark i:nth-child(5) { height: 45%; animation-delay: 0.6s; }
  @keyframes navpulse {
    0%, 100% { transform: scaleY(0.5); opacity: 0.6; }
    50% { transform: scaleY(1); opacity: 1; }
  }
  .brand-name {
    font-weight: 700;
    font-size: 18px;
    letter-spacing: -0.03em;
  }
  .brand-sub {
    font-size: 12px;
    color: var(--muted);
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: 24px;
    font-size: 14px;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .nav-links::-webkit-scrollbar {
    display: none;
  }
  .nav-links a {
    text-decoration: none;
    color: var(--muted);
    transition: color 200ms var(--ease), transform 200ms var(--ease);
  }
  .nav-links a:hover {
    color: var(--ink);
  }
  .nav-links a:active {
    transform: scale(0.98);
  }
  .nav-cta {
    color: var(--brass) !important;
  }
  @media (max-width: 560px) {
    .nav-pill {
      gap: 16px;
    }
    .brand-sub {
      display: none;
    }
    .nav-links {
      gap: 16px;
    }
  }

  /* ---------- hero ---------- */
  .hero {
    padding-top: 96px;
  }
  .hero-inner {
    max-width: 900px;
  }
  .hero-title {
    font-size: 36px;
    line-height: 1.05;
    font-weight: 700;
    margin-top: 24px;
    max-width: 680px;
    /* the one sanctioned gradient: text only, never a background */
    background: linear-gradient(90deg, #ffffff 0%, #9b9b9b 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
  }
  @media (min-width: 640px) {
    .hero-title {
      font-size: 48px;
    }
  }
  @media (min-width: 1024px) {
    .hero {
      padding-top: 160px;
    }
    .hero-title {
      font-size: 60px;
    }
  }
  .hero-body {
    margin-top: 24px;
    max-width: 680px;
    font-size: 18px;
    line-height: 28px;
    color: var(--muted);
  }
  .kw {
    color: var(--ink);
    background: var(--surface-2);
    padding: 2px 8px;
    border-radius: 8px;
    font-size: 14px;
  }
  .hero-cta {
    margin-top: 32px;
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
  .hero-cta.center {
    justify-content: center;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 16px;
    font-weight: 600;
    padding: 8px 12px;
    border-radius: 8px;
    text-decoration: none;
    transition:
      transform 250ms var(--ease),
      background 250ms var(--ease),
      border-color 250ms var(--ease),
      color 250ms var(--ease);
  }
  .btn:active {
    transform: scale(0.98);
  }
  .btn-primary {
    background: var(--brass);
    color: #181818;
    border: 1px solid var(--brass);
  }
  .btn-primary:hover {
    transform: translateY(-2px);
    background: #f2ba63;
  }
  .btn-ghost {
    background: transparent;
    color: var(--ink);
    border: 1px solid var(--line);
  }
  .btn-ghost:hover {
    border-color: var(--brass);
    color: var(--brass);
    transform: translateY(-2px);
  }
  .proof {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 8px 24px;
    padding: 0;
    margin: 32px 0 0;
    font-size: 12px;
    color: var(--muted);
    letter-spacing: 0.05em;
  }
  .proof li {
    display: flex;
    align-items: center;
    gap: 24px;
  }
  .proof li:not(:last-child)::after {
    content: "·";
    color: var(--brass);
  }

  /* hero scope */
  .hero-scope {
    margin-top: 48px;
    padding-bottom: 48px;
  }
  .scope {
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--surface);
    padding: 12px 16px;
    position: relative;
    overflow: hidden;
  }
  .scope-label {
    position: relative;
    font-size: 12px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
  }
  .scope-label .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--cyan);
    box-shadow: 0 0 10px var(--cyan);
  }
  .wave-slot {
    position: relative;
    height: clamp(90px, 16vw, 150px);
  }
  .scope-ticks {
    position: relative;
    display: flex;
    justify-content: space-between;
    margin-top: 8px;
    font-size: 12px;
    color: var(--muted);
    border-top: 1px solid var(--line-soft);
    padding-top: 8px;
  }

  /* ---------- headings ---------- */
  .head-block {
    max-width: 680px;
    margin-bottom: 32px;
  }
  .h2 {
    font-size: 30px;
    line-height: 36px;
    margin-top: 12px;
  }
  @media (min-width: 768px) {
    .h2 {
      font-size: 36px;
      line-height: 40px;
    }
  }
  .head-sub {
    margin-top: 16px;
    max-width: 680px;
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }

  /* ---------- input/output strip ---------- */
  .strip-sec {
    border-top: 1px solid var(--line-soft);
    border-bottom: 1px solid var(--line-soft);
  }
  .strip {
    display: flex;
    align-items: stretch;
    flex-wrap: wrap;
    gap: 8px;
  }
  .strip-node {
    flex: 1 1 190px;
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    transition: background 250ms var(--ease), border-color 250ms var(--ease);
  }
  .strip-node:hover {
    background: var(--surface-2);
  }
  .strip-node.out {
    border-color: color-mix(in srgb, var(--brass) 45%, var(--line));
  }
  .strip-k {
    font-size: 12px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--cyan);
  }
  .strip-node.out .strip-k {
    color: var(--brass);
  }
  .strip-v {
    font-weight: 600;
    font-size: 16px;
    line-height: 24px;
  }
  .strip-note {
    font-size: 14px;
    line-height: 20px;
    color: var(--muted);
  }
  .strip-arrow {
    display: flex;
    align-items: center;
    color: var(--dim);
    font-size: 18px;
  }
  @media (max-width: 720px) {
    .strip-arrow {
      transform: rotate(90deg);
      justify-content: center;
      width: 100%;
    }
    .strip-node {
      flex-basis: 100%;
    }
  }
  .strip-caption {
    margin-top: 24px;
    max-width: 680px;
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }

  /* ---------- tagline reveal ---------- */
  .tagline-sec {
    padding-block: 80px;
  }
  .tagline {
    font-size: 36px;
    line-height: 40px;
    font-weight: 600;
    letter-spacing: -0.02em;
    max-width: 680px;
    text-wrap: balance;
  }
  @media (min-width: 768px) {
    .tagline {
      font-size: 48px;
      line-height: 1.1;
    }
  }
  [data-word] {
    color: rgba(245, 245, 245, 0.3);
    transition: color 600ms var(--ease);
    transition-delay: calc(var(--i) * 70ms);
  }
  [data-tagline].play [data-word] {
    color: var(--ink);
  }
  @media (prefers-reduced-motion: reduce) {
    [data-word] {
      transition-delay: 0s;
    }
  }

  /* ---------- features ---------- */
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1px;
    background: var(--line-soft);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    overflow: hidden;
  }
  .card {
    background: var(--surface);
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    transition: background 250ms var(--ease);
  }
  .card:hover {
    background: var(--surface-2);
  }
  .feature-grid .card:nth-child(2) { transition-delay: 70ms; }
  .feature-grid .card:nth-child(3) { transition-delay: 140ms; }
  .feature-grid .card:nth-child(4) { transition-delay: 210ms; }
  .card-tag {
    align-self: flex-start;
    font-size: 12px;
    line-height: 16px;
    letter-spacing: 0.05em;
    color: var(--brass);
    border: 1px solid color-mix(in srgb, var(--brass) 35%, transparent);
    border-radius: 999px;
    padding: 4px 12px;
  }
  .card-title {
    font-size: 20px;
    line-height: 28px;
    font-weight: 600;
    margin-top: 4px;
  }
  .card-body {
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }

  /* ---------- workflow ---------- */
  .workflow-sec {
    border-top: 1px solid var(--line-soft);
    border-bottom: 1px solid var(--line-soft);
  }
  .loop-legend {
    display: flex;
    gap: 24px;
    margin-top: 12px;
    font-size: 12px;
    line-height: 16px;
    color: var(--muted);
  }
  .loop-legend span {
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .lg {
    width: 24px;
    height: 3px;
    border-radius: 2px;
    display: inline-block;
  }
  .lg-signal {
    background: var(--cyan);
    opacity: 0.6;
  }
  .lg-loop {
    background: repeating-linear-gradient(90deg, var(--coral) 0 4px, transparent 4px 7px);
  }

  /* ---------- code panes ---------- */
  .code-pane {
    margin: 0;
    border: 1px solid var(--line);
    border-radius: 12px;
    overflow: hidden;
    background: #000000;
  }
  .pane-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    font-size: 12px;
    line-height: 16px;
    color: var(--muted);
    background: var(--surface);
    border-bottom: 1px solid var(--line);
  }
  .pane-dots {
    display: inline-flex;
    gap: 4px;
  }
  .pane-dots i {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--raised);
  }
  .pane-tree { color: var(--brass); }
  .code {
    margin: 0;
    padding: 16px 24px;
    font-family: var(--font-mono);
    font-size: 14px;
    line-height: 1.7;
    overflow-x: auto;
    color: #d4d4d4;
  }
  .code code {
    font-family: inherit;
  }
  .c-h { color: var(--brass); font-weight: 500; }
  .c-q { color: #bfbfbf; }
  .c-mark { color: var(--coral); }
  .c-c { color: var(--dim); }
  .c-p { color: var(--cyan); }
  .c-flag { color: var(--brass); }
  .tree { color: #8f8f8f; }
  .t-dir { color: var(--cyan); }
  .t-file { color: #d4d4d4; }
  .t-raw { color: var(--dim); }
  .t-full { color: var(--brass); font-weight: 500; }
  .t-meta { color: var(--dim); }

  .script-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    align-items: start;
  }
  @media (max-width: 860px) {
    .script-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ---------- quickstart ---------- */
  .start-sec {
    border-top: 1px solid var(--line-soft);
  }
  .start-grid,
  .arch-grid {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: 48px;
    align-items: center;
  }
  @media (max-width: 860px) {
    .start-grid,
    .arch-grid {
      grid-template-columns: 1fr;
    }
  }
  .cmd-list {
    margin-top: 32px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .cmd-lead {
    font-size: 12px;
    line-height: 16px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--dim);
    margin-bottom: 4px;
  }
  .cmd {
    display: flex;
    gap: 16px;
    align-items: baseline;
    padding-bottom: 8px;
    border-bottom: 1px solid var(--line-soft);
  }
  .cmd code {
    color: var(--brass);
    font-size: 14px;
    line-height: 20px;
    flex: 0 0 auto;
    min-width: 136px;
  }
  .cmd span {
    color: var(--muted);
    font-size: 14px;
    line-height: 20px;
  }

  /* ---------- architecture ---------- */
  .arch-sec {
    border-top: 1px solid var(--line-soft);
  }
  .arch-list {
    margin: 24px 0 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .arch-list li {
    position: relative;
    padding-left: 24px;
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }
  .arch-list li::before {
    content: "▸";
    position: absolute;
    left: 0;
    color: var(--brass);
  }
  .copyout {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 24px;
  }
  .copyout-title {
    font-weight: 600;
    font-size: 20px;
    line-height: 28px;
    letter-spacing: -0.01em;
  }
  .copyout-body {
    margin-top: 8px;
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }
  .copyout .code-pane {
    margin-top: 16px;
  }
  .term.small .code {
    font-size: 12px;
    line-height: 16px;
  }

  /* ---------- fit ---------- */
  .fit-sec {
    border-top: 1px solid var(--line-soft);
    border-bottom: 1px solid var(--line-soft);
  }
  .fit-inner {
    display: grid;
    gap: 24px;
  }
  .fit-head {
    max-width: 680px;
  }
  .fit-body {
    max-width: 680px;
    color: var(--muted);
    font-size: 18px;
    line-height: 28px;
  }
  .chips {
    list-style: none;
    padding: 0;
    margin: 8px 0 0;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .chips li {
    font-size: 12px;
    line-height: 16px;
    color: var(--ink);
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 8px 16px;
  }

  /* ---------- docs ---------- */
  .doc-list {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--line);
  }
  .doc-row {
    display: grid;
    grid-template-columns: auto 1fr auto auto;
    align-items: center;
    gap: 16px;
    padding: 16px;
    border-bottom: 1px solid var(--line);
    text-decoration: none;
    transition: padding 250ms var(--ease), background 250ms var(--ease);
  }
  .doc-row:hover {
    padding-left: 24px;
    background: color-mix(in srgb, var(--brass) 6%, transparent);
  }
  .doc-row:active {
    transform: scale(0.995);
  }
  .doc-idx {
    font-size: 12px;
    line-height: 16px;
    color: var(--brass);
  }
  .doc-label {
    font-weight: 600;
    font-size: 16px;
    line-height: 24px;
  }
  .doc-meta {
    font-size: 12px;
    line-height: 16px;
    color: var(--muted);
  }
  .doc-arrow {
    color: var(--dim);
    transition: color 250ms var(--ease);
  }
  .doc-row:hover .doc-arrow {
    color: var(--brass);
  }
  @media (max-width: 560px) {
    .doc-meta {
      display: none;
    }
  }

  /* ---------- faq ---------- */
  .faq-sec {
    border-top: 1px solid var(--line-soft);
  }
  .faq-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    align-items: start;
  }
  @media (max-width: 768px) {
    .faq-grid {
      grid-template-columns: 1fr;
    }
  }
  .faq-item {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 24px;
  }
  .faq-q {
    font-size: 18px;
    line-height: 28px;
    font-weight: 600;
  }
  .faq-a {
    margin-top: 8px;
    color: var(--muted);
    font-size: 16px;
    line-height: 24px;
  }

  /* ---------- final cta ---------- */
  .cta-sec {
    text-align: center;
    border-top: 1px solid var(--line-soft);
  }
  .cta-inner {
    max-width: 720px;
  }
  .cta-wave {
    height: 60px;
    max-width: 420px;
    margin: 0 auto 32px;
    opacity: 0.85;
    -webkit-mask-image: linear-gradient(90deg, transparent, #000 20%, #000 80%, transparent);
    mask-image: linear-gradient(90deg, transparent, #000 20%, #000 80%, transparent);
  }
  .cta-wave.wave-slot {
    margin-left: auto;
    margin-right: auto;
  }
  .cta-title {
    font-size: 30px;
    line-height: 36px;
  }
  @media (min-width: 768px) {
    .cta-title {
      font-size: 48px;
      line-height: 1.1;
    }
  }
  .cta-body {
    margin: 16px auto 0;
    max-width: 560px;
    color: var(--muted);
    font-size: 18px;
    line-height: 28px;
  }
  .cta-sec .hero-cta {
    margin-top: 32px;
    justify-content: center;
  }

  /* ---------- footer ---------- */
  .foot {
    border-top: 1px solid var(--line);
    padding-block: 32px;
  }
  .foot-inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }
  .foot-brand {
    display: inline-flex;
    align-items: baseline;
    gap: 8px;
  }
  .foot-note {
    font-size: 12px;
    line-height: 16px;
    color: var(--dim);
  }
  .foot-note a {
    color: var(--muted);
    text-decoration: none;
    transition: color 200ms var(--ease);
  }
  .foot-note a:hover {
    color: var(--brass);
  }
  .foot-link {
    font-size: 12px;
    line-height: 16px;
    color: var(--muted);
    text-decoration: none;
    transition: color 200ms var(--ease);
  }
  .foot-link:hover {
    color: var(--brass);
  }
</style>
